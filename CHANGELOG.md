# Changelog

このプロジェクトは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## 0.1.0 — 2026-03-29

### Added

- `FolderTree` — 読み取り専用のフォルダ／ファイルツリー UI（`variant`、`showIcons`、`showConnectorLines` など）
- 既定 SVG アイコン `FolderTreeDefaultFolderIcon` / `FolderTreeDefaultFileIcon`
- スタイル配布サブパス `folder-tree-ui/styles.css`（`exports`）
- 記事用サンプル `REPO_TREE_ROOT` と `RepoFolderTree`
- ESM / CJS デュアル出力と、`import` / `require` それぞれに対応する型定義（`.d.ts` / `.d.cts`）
