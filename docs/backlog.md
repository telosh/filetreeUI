# バックログ（アイデア置き場）

**ここに書いてあることは未コミットの約束ではありません。** 実装する際は [feature-matrix.md](./feature-matrix.md) に移し、README / CHANGELOG を更新してください。

優先度はこのファイルでは固定しません。Issue やプロジェクトボードで付け替えて構いません。

## 表示パターン・バリエーション

- [ ] プリセットテーマクラス（例: `ft-theme-dark`）を CSS で同梱するかドキュメントのみか
- [ ] `variant` の追加（例: `minimal` = 罫線なし・余白のみ 等）
- [ ] ルートなしでも「森」表示（複数ルートノード）— API は `root` 単体のまま `children` で擬似するか、配列 API か
- [ ] 行ホバー／選択風の見た目用オプション（状態管理は利用側）

## データ・ユーティリティ

- [ ] `paths: string[]` から `FolderTreeNode` を生成するヘルパー
- [ ] `.gitignore` 風のフィルタでツリーを間引くヘルパー（ドキュメント用）
- [ ] 大きいツリー向け: 折りたたみ状態を外部から渡す（読み取り専用でも「深さ N まで」など）

## カスタマイズ API

- [ ] 行全体の `renderRow`（アイコン＋ラベル以外を差し込む）
- [ ] ファイル拡張子に応じたアイコンのプリセット（任意依存を増やさない形）
- [ ] ノードに `href` やメタデータを載せられる拡張型（後方互換をどうするか要設計）

## アクセシビリティ・インタラクション

- [ ] 読み取り専用のままフォーカス移動のみサポートするかどうかのスパイク
- [ ] 完全な `role="tree"` + 開閉は別パッケージ `folder-tree-ui-a11y` のような分割案の検討

## 開発体験

- [ ] Storybook または playground 内の「パターン一覧」ページ
- [ ] ビジュアル回帰（任意）

## メモ

スコープの議論は [product-scope.md](./product-scope.md) を正とする。ここは発散を許容するリストです。
